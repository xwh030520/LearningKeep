# （一）数字
在JS里面，数字均为双精度浮点类型（double），除了能够表示浮点数，数字类型也还能表示三种符号值：
- +Infinity（正无穷）
- -Infinity（负无穷）
- NaN（not-a-number,非数字）
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;`双精度归一化二进制形式：（-1）e（符号位）*（1.尾数位）*2e（实际指数）`
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;1位符号位+11位指数位（普通储值范围1-2046）+52位尾数。其中52位尾数部分还应加上一位隐含值1，就是53位有效位，但是这个隐含值1不占用二进制存储空间，默认在即可。
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;注意：实际指数+偏移量（1023）=指数位存储值
## 十进制数字
十进制可以以0开头，后面接其他十进制数字，但是若下一个接的十进制数字小于8，那么该数字将会被当做八进制处理。

	0888 以十进制解析
	0777 以八进制解析
## 二进制数字
二进制数字语法是以0为开头，后面接一个小写或大写的拉丁文字母B（0B或者0b）。

		0b... 或者 0B...
## 八进制数字
八进制数字语法是以0为开头，后面接一个小写或大写的拉丁文字母O（0O或者0o）。若0后面的数字不在0-7的范围内，该数字将会被转换成十进制数字。

		0O... 或者 0o...
## 十六进制数字
十六进制数字语法是以0为开头，后面接一个小写或大写的拉丁文字母X（0x或者0X）。

		0X... 或者 0x...
## 指数形式
在JS中，指数形式用e或者E连接尾数和指数，语法格式：

		尾数e指数  等价于尾数 * 10^指数
		尾数E指数  e/E大小写均可，效果一致
		175e-2 = 1.75
# （二）数字对象
- [[API#数字对象| 数字对象API]]
## （三）数学对象

